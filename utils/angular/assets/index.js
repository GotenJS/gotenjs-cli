const path = require("path");

const files = {
    gotenLogo: "Goten.png",
};

const path_assets = __dirname + path.sep;

const imagenes = {
    gotenLogo: path_assets + files.gotenLogo,
};

module.exports = imagenes;